import classNames from 'classnames';
import styles from './styles.module.css';

export const Objects = () => {
    return <div className={styles.objects}>
        <h2 className={styles.title}>Object component</h2>
    </div>
}